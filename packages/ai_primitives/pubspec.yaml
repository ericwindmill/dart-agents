name: ai_primitives
description: >-
  Framework-agnostic data types for AI/LLM interactions in Dart — messages,
  content parts, tools, and usage — with no dependency on any model
  provider or agent framework.
version: 0.1.0

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  equatable: ^2.0.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
